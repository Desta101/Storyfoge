import ProjectClient from "./ProjectClient";

export const metadata = {
  title: "Project",
  description: "StoryForge project details.",
};

export default function ProjectPage() {
  return <ProjectClient />;
}

